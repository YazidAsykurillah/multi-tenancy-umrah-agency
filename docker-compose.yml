services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    networks:
      - umrah-erp-network
      - web-gateway # Tambahkan ini

  # Nginx (Official Image)
  web-umrah-erp:
    image: nginx:alpine
    volumes:
      - .:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - umrah-erp-network
      - web-gateway # Tambahkan ini

  # Database MySQL 8.0
  db:
    container_name: db-umrah-erp
    image: mysql:8.0
    restart: always
    environment:
      - MYSQL_DATABASE=umrah_erp
      - MYSQL_ROOT_PASSWORD=secret
      - MYSQL_USER=yazid
      - MYSQL_PASSWORD=secret
    volumes:
      - db-data-umrah-erp:/var/lib/mysql
    networks:
      - umrah-erp-network
  # Tambahkan phpMyAdmin di sini
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: phpmyadmin_umrah_erp
    links:
      - db
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "8090:80"
    networks:
      - umrah-erp-network

networks:
  umrah-erp-network:
    driver: bridge
  web-gateway: # Daftarkan network global di sini
    external: true

volumes:
  db-data-umrah-erp: